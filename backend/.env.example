# Leave unset to use SQLite at project data/weather.db (recommended for local dev).
# For PostgreSQL (e.g. production on Render), uncomment and adjust:
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME
